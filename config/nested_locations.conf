server {
    listen 8080;
    host 127.0.0.1;
    root ../www;

    location /img {
        root ../media;
        index gallery.html;
    }

    location /upload {
        methods POST;
        return 301 /success.html;
    }
}
