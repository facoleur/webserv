server {
    listen 8080;
    host 127.0.0.1;
    root nonexistent;
    index index.html index.htm;
    methods GET POST;

    location /upload {
        root ../uploads;
        methods POST;
    }
}
