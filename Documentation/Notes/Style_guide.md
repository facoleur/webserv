# Webserv organisation and code style guide

## Repo structure
- src/, inc/ ?

## Clang-format
- ? style

## Headers in files
- `// HeaderName.hpp` or `// FileName.cpp` at the top

## Pragma once instead of ifndef
