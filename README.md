# Webserv
Repo for @lferro, @tgajdov and @fallan's webserv project @ Ecole 42 Lausanne.

## Description
An HTTP server in C++98, (not strictly) following HTTP/1.1.

## Usage
- `make` to build the project
- `./webserv` to start the server
